function BinIndex(props) {
    // const bin_arr = props.bins.map(bin => {
    //     return (
    //         <li key={bin.id}>
    //             <h2>{bin.title}</h2>
    //             <img src={bin.photoUrl} alt="" />
    //         </li>
    //     );
    // })

    return (
        <ul>
            {/* {bin_arr} */}
        </ul>
    );
}

export default BinIndex;